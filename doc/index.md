# Patterns
```
use es syntax instead of plain js => requiring webpack,babel

use jsx instead of html => requiring vuetify-loader

use api => requiring axios and express

global API processing and erroring status => requiring message showing

express => requiring api-arg-validation

need to store user status and preference => login, mysql

need to somehow set page param into URL query => ? 

need to eliminate login => using browser identity

```